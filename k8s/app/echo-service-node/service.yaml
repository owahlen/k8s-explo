apiVersion: v1
kind: Service
metadata:
  name: echo-service-node
  labels:
    app: echo-service-node
spec:
  selector:
    app: echo-service-node
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
